class NetworkCommunication:
    def communicate(self, device_ip, protocol):
        # Logic for communication with network devices over different protocols
        print(f"Communicating with {device_ip} using {protocol}.")
