import pandas as pd

class Database:
    def __init__(self, config):
        self.config = config
        # Connect to the database (implementation not shown)

    def save_data(self, data):
        # Implement logic to save data to the database
        print("Data saved to the database.")
